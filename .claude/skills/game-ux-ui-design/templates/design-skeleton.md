# {게임명} UI/UX 디자인 문서 — {기능 / 화면군}

> 복사해서 채우는 문서 스켈레톤. `{...}`를 GDD 근거로 채운다. 와이어프레임은 `wireframes/*.svg`로
> 분리, 레퍼런스 이미지는 `references/ui/...`에 **별도 파일 링크**로 첨부(임베드 금지).
> 작성 규칙·근거: `references/design-structure.md`, `references/ux-heuristics.md`.

---

## 0. 표지

| 항목 | 값 |
| --- | --- |
| 문서명 | {게임명} UI/UX 디자인 문서 — {화면군} |
| 게임 / 빌드 | {game} / {milestone, e.g. Vertical Slice} |
| 버전 / 날짜 / 작성자 | v0.1 / {date} / {author} |
| 상태 | draft · review · approved 중 하나 |
| 기준 GDD | `{path}` ({섹션 refs}) |

## 1. 개요 및 목표

- **이 문서가 푸는 문제**: {what UI problem this design answers}
- **핵심 경험 목표**: {1–2문장, GDD §핵심 경험에서}
- **측정 가능 성공 지표**: {testable, GDD 통과 기준에서 — e.g. "어느 순간에도 7시까지 남은 시간을 1초 내 인지"}
- **포함 범위 / 제외 범위**: {in / out of scope screens}

## 2. 사용자 및 맥락

- **플레이어 페르소나**: {target audience, skill range — 초보~숙련}
- **플랫폼 / 입력장치**: {PC·Steam / 키보드·마우스 + 패드 (+터치?)}
- **진입 맥락 / 가정**: {when/where each screen is encountered}

### 2-1. 디자인 원칙 (이 게임 UI가 모든 화면에서 지키는 것)
각 화면의 〈UX 설계 의도〉는 이 원칙에 맞춰 점검한다. (작성 근거 방법론은 부록 참조.)

| # | 원칙 | 이 게임에서의 의미 | 안 지키면 |
| --- | --- | --- | --- |
| P1 | 피드백 즉시 | {모든 행동·상태 변화에 바로 반응(소리·표시)} | {손맛·상황 인지 약화} |
| P2 | 위험 가독성 | {플레이어·위협·획득물 순으로 잘 보이게} | {위험이 안 보임} |
| P3 | 혼잡해도 잘 보이게 | {적·탄막이 많아도 핵심은 항상 구분} | {즉사 패턴이 묻힘} |
| P4 | 일관성 | {위치·색·아이콘·조작을 화면마다 통일} | {매번 헷갈림} |
| P5 | 처음엔 적게 | {핵심부터, 나머지는 필요할 때} | {초보 과부하} |
| P6 | 접근성 | {색 단독 금지, 자막, 흔들림 토글, 리매핑} | {색약·민감 사용자 배제} |

## 3. 화면 흐름도 (wireflow)

```text
{paste GDD screen flow; 각 노드에 게임 STATE와 디자인 문서 섹션 번호 주석}
```
![screen flow](wireframes/flow.svg)

| 전이 | 트리거(이벤트/조건) | 도착 화면 | 상태↔HUD |
| --- | --- | --- | --- |
| {Title→Main} | {any key} | {Main Menu} | {HUD 숨김} |

### 3-1. 레퍼런스 개요
와이어프레임이 참고한 인지도 높은 게임 UI. **실제 이미지+설명은 각 화면 본문 `#### 참조`에 인라인
임베드**(별도 파일·링크 금지 — PDF/Word에서 안 열림). 아래는 한눈에 보는 개요표.
전체 수집 후보·절차는 부록 REFERENCE_BOARD.

| 화면 유형 | 참고 게임 / 출처 | 차용 포인트 | 본문 |
| --- | --- | --- | --- |
| 인게임 HUD | {Vampire Survivors, Dead Cells} | {timer 위치, 4-slot…} | §5.1 참조 |

## 4. 글로벌 컴포넌트 인벤토리

재사용 컴포넌트를 한 번 정의하고 화면별로 코드 참조. variant·토큰 포함.

| 코드 | 컴포넌트 | variant | 토큰(color/type/spacing) | 상태 매트릭스 |
| --- | --- | --- | --- | --- |
| C-CARD | 선택 카드 | common/uncommon/rare/lucid | {…} | §6 또는 화면별 |
| C-BAR | 가로 미터 | hp/xp/purge/boss | {…} | — |

## 5. 화면별 명세

> 각 화면 = `per-screen.md` 블록. 순서: 목적 → 참조 → 와이어프레임 → 범례 → 상태 매트릭스 →
> 입력 패리티 → 데이터 바인딩 → 내비게이션 → 엣지케이스 → 접근성 → **UX 설계 의도** → 미해결 질문.

- 5.1 {인게임 HUD}
- 5.2 {레벨업 / 아이템 선택}
- 5.3 {보스 보상 (스킬트리 + 아이템 4택)}
- 5.4 {결과 / 요약 (승·패 분기)}
- 5.5 {보스전 HUD 변형}
- 5.6 {캐릭터 선택} …

## 6. 공통 규칙 (cross-cutting)

- **내비게이션 모델**: {포커스 이동, 기본 선택, 백/취소, 스틱 vs 마우스}
- **글로벌 empty / loading / error 패턴**: {공통 처리}
- **접근성 글로벌**: {색각 대체 팔레트, 화면흔들림/플래시 토글, 판정표시, 컷인 간소화, 자막 — GDD §접근성}
- **레이아웃 / 세이프 영역**: {16:9 기준, 21:9·4:3 대응, HUD 스케일}

### 6-1. UX 설계 의도 종합
화면을 가로지르는 UX 위험과 결정. GDD 리스크 레지스터와 직접 연결.

| UX 위험 (GDD 리스크 연계) | 화면 | 결정 / 완화 | 검증 방법 |
| --- | --- | --- | --- |
| {화면 물량이 많아 위험이 안 보임} | HUD, 보스전 | {색/형태/소리 계층화} | {색각 모드 플레이테스트} |

## 7. 미해결 질문

화면별 〈미해결 질문〉을 여기로 집계. 살아있는 섹션.

## 8. 버전 이력

| 버전 | 날짜 | 변경 |
| --- | --- | --- |
| v0.1 | {date} | 초안 |

## 부록

- 전체 흐름 다이어그램 / 사이트맵
- references/ui 매니페스트: `ui_research/manifests/local_ui_refs_manifest.md`
