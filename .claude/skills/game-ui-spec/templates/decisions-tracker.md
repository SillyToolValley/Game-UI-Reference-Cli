# {게임명} — UI/UX 수치·결정 트래커 (living doc) — TEMPLATE

> 스펙의 모든 수치·미정 결정을 **출처별로 분류**해 출하 전 확정을 추적(Process 9).
> 본문 GWT/범례의 `[확정 필요]`·추정 수치를 여기로 집계한다.
> 출처: **`GDD확정`**(GDD 줄 인용) · **`표준`**(WCAG/플랫폼/접근성) · **`추정`**(작성자 제안 — 디자인/플레이테스트 확정 필요).
> 상태: `OPEN` · `PROPOSED`(제안 기본값 있음) · `LOCKED`(확정).

## 1. GDD 확정 수치 (그대로 사용)
| 값 | GDD § | UI 사용처 |
| --- | --- | --- |
| {…} | {§} | {요소 코드} |

## 2. 표준 수치 (근거 명시 — 확정)
| 값 | 표준 | 사용처 | 상태 |
| --- | --- | --- | --- |
| 본문 대비 ≥ 4.5:1 | WCAG 2.2 AA | 전 텍스트 | LOCKED |
| UI/포커스 ≥ 3:1 + 색 외 표식 | WCAG / Game A11y | 포커스·바 | LOCKED |
| 색 단독 금지 | Game Accessibility Guidelines | 전 상태 | LOCKED |

## 3. 추정 수치 — 확정 필요 (제안 기본값 포함)
| # | 항목 | 화면 | 현재(제안) | 출처 | 상태 | 확정 방법 |
| --- | --- | --- | --- | --- | --- | --- |
| D-01 | {저HP 임계 등} | {화면} | {값} | 추정 | OPEN | {플레이테스트/GDD 결정} |

## 4. GDD 추가 필요(데이터 계약)
스펙의 "UI 제안" 이벤트/런타임 상태 필드 목록 → TDD 인계 시 GDD §12 추가.

## 5. 규칙
- 모든 `OPEN`은 출하 전 `LOCKED`. 추정 수치는 이 트래커에서만 변경, 스펙은 참조.
- 스펙에 새 수치를 넣을 때 출처 라벨(GDD확정/표준/추정)을 달고 여기 등록.
